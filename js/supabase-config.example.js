/**
 * 使用步骤：
 * 1. 复制本文件为同目录下的 supabase-config.js（文件名必须一致）
 * 2. 在 Supabase 项目 Settings → API 中填入 Project URL 与 anon public key
 * 3. 将 supabase-config.js 与网页一起部署（anon key 可公开，但勿泄露 service_role）
 */
window.__STUDIO_SUPABASE = {
  url: "",
  anonKey: ""
};
